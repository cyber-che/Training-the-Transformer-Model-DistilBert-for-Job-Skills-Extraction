#Run pip install -r requirements.txt to install dependencies

#dependencies
numpy==1.24.2
pandas==1.5.3
matplotlib==3.7.0
scikit-learn==1.2.2
scipy==1.10.1
re==2.2.1  
torch==2.1.0+cpu  # If you are not using GPU, otherwise you can use torch with cuda support
transformers==4.30.0
scikit-learn==1.2.2

# If you are using a GPU (CUDA version-specific)
torch==2.1.0+cu118  # Modify this line if you would like to use a different cuda version for training
